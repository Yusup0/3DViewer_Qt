#include "functional_viewer.h"

void move_x(s_info *data_, int shift_) {
    ft_unuased(shift_);
    ft_unuased(data_);
}

void move_y(s_info *data_, int shift_) {
    ft_unuased(shift_);
    ft_unuased(data_);
}

void move_z(s_info *data_, int shift_) {
    ft_unuased(shift_);
    ft_unuased(data_);
}

void scale(s_info *data_, float scale_) {
    ft_unuased(scale_);
    ft_unuased(data_);
}

void rotate_x(s_info *data_, double turn_){
    ft_unuased(turn_);
    ft_unuased(data_);
}

void rotate_y(s_info *data_, double turn_) {
    ft_unuased(turn_);
    ft_unuased(data_);
}

void rotate_z(s_info *data_, double turn_) {
    ft_unuased(turn_);
    ft_unuased(data_);
}
